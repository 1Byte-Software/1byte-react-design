import { RangePickerStyles } from './styles';
import { RdRangePickerProps } from './types';

export const RangePicker = (props: RdRangePickerProps) => {
    return <RangePickerStyles {...props} />;
};
