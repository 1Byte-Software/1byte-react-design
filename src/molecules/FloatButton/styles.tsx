import styled from '@emotion/styled';
import { FloatButton } from 'antd';

export const FloatButtonStyles = styled(FloatButton)``;

export const FloatButtonGroupStyles = styled(FloatButton.Group)``;

export const BackTopStyles = styled(FloatButton.BackTop)``;
