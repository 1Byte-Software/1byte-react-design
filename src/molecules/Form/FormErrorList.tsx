import { FormErrorListStyles } from './styles';
import { RdFormErrorListComponent } from './types';

export const FormErrorList: RdFormErrorListComponent = props => {
    return <FormErrorListStyles {...props} />;
};
