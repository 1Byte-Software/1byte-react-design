import styled from '@emotion/styled';
import { Statistic } from 'antd';
import { RdStatisticProps } from './types';

export const StatisticStyles = styled(Statistic as React.FC<RdStatisticProps>)``;

export const TimerStyles = styled(Statistic.Timer)``;
