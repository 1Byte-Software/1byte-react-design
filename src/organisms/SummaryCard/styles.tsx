import styled from '@emotion/styled';
import { Card } from '../../molecules';
import { Statistic } from '../../molecules/Statistic';

export const SummaryCardWrapper = styled(Card)``;

export const StatisticWrapper = styled(Statistic)``;
